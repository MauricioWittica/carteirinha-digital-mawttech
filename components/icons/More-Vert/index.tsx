import React from "react";
import { Feather } from '@expo/vector-icons';
import { StyleSheet } from "react-native";

export const MoreVertIcon = (props) => { 

return(

    <Feather name="more-vertical" size={props.size} color={props.color}/>
);


  
}