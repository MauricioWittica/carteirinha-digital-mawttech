import React from "react";
import {Feather } from '@expo/vector-icons'; 


export const InfoCirlceIcon = (props) => { 

return(

    <Feather name="info" size={props.size} color={props.color}/>
);
    
}