import React from "react";
import {Feather } from '@expo/vector-icons'; 


export const ChevronLeftIcon = (props) => { 

return(

    <Feather name="chevron-left" size={props.size} color={props.color}/>
);
    
}