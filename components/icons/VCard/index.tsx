import React from "react";
import {Entypo} from '@expo/vector-icons';

export const VCardIcon = (props) => { 

return(

  <>
  <Entypo name="v-card" size={props.size} color={props.color}/>
  </>

);
    
}