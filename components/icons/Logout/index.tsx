import React from "react";
import { MaterialCommunityIcons } from '@expo/vector-icons';

export const LogoutIcon = (props) => {

    return(
<MaterialCommunityIcons name="logout" size={props.size} color={props.color} />

    );

}