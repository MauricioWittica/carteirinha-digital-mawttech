import React from "react";
import { AntDesign } from '@expo/vector-icons';

export const LogoutIcon = (props) => {

    return(
<AntDesign name="logout" size={props.size} color={props.color} />

    );

}