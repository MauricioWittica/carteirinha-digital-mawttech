import React from "react";
import {MaterialCommunityIcons } from '@expo/vector-icons'; 


export const ImageEditIcon = (props) => { 

return(

    <MaterialCommunityIcons name="image-edit" size={props.size} color={props.color}/>
);
    
}